{
  "system": "Effect / Resistance",
  "description": "Every skill effect has a type. Every interactable has an effect value. The SAME variable is used on caster (multiplies) and target (divides).",
  "formula": "result = base × caster.effect / target.effect",
  "effect_types": {
    "push": {
      "variable": "push_effect",
      "caster_behavior": "multiplies push force",
      "target_behavior": "divides push force",
      "default": 1.0
    },
    "damage": {
      "variable": "vitality",
      "description": "Direct HP damage. Applied at the same time as push when a hit skill connects.",
      "caster_behavior": "multiplies base damage",
      "target_behavior": "divides base damage (higher vitality = more resistant)",
      "default": 1.0,
      "formula": "final_damage = base_damage × caster.vitality / target.vitality"
    }
  }
}
